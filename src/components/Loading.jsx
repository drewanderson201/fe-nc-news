
export default function Loading() {

    return (
      <div>
        <p>Loading, please wait...</p>
        <p>This application can take a few mins to load if it hasn't been used in a while.</p>
      </div>
    );
}
